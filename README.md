# Wellcome to my DotFiles 

Here's a quick tutorital

Once you boot a fresh ArchLinux install, just run the following command and it should install all that i normally use with the config files
```
sudo pacman -S git
git clone https://github.com/ZenithSlayer/.zenith-Dotfiles.git
cd .zenith-Dotfiles
chmod +x install_software.sh
intall_software.sh
```
so the script can run